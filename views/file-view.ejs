<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View File</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light d-flex align-items-center justify-content-center vh-100">

    <div class="container text-center">
        <h2 class="text-primary fw-bold">📂 Viewing File</h2>
        <hr>

        <!-- File Embed with Correct Page Jump -->
        <% if (note.filePath.endsWith('.pdf')) { %>
            <iframe src="/<%= note.filePath %>#page=<%= pageNumber %>" width="100%" height="600px"></iframe>
        <% } else if (note.filePath.endsWith('.ppt') || note.filePath.endsWith('.pptx')) { %>
            <iframe src="https://view.officeapps.live.com/op/embed.aspx?src=http://localhost:3000/<%= note.filePath %>#Slide=<%= pageNumber %>" width="100%" height="600px"></iframe>
        <% } else { %>
            <p class="text-muted">Unsupported file type. <a href="/<%= note.filePath %>" target="_blank">Download File</a></p>
        <% } %>

        <!-- Back Button -->
        <div class="mt-4">
            <a href="/" class="btn btn-secondary btn-lg">⬅ Back to Notes</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
